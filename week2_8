#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n, sum = 0;
    scanf("%d", &n);
    char *list = (char*)malloc(sizeof(char)*n);
    scanf("%s", list);
    for (int i = 0; i < n; i++)
    {
        sum += ((int)list[i] - 48);
    }
    printf("%d", sum);
}
