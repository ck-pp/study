// 2869번 (제한시간이 짧은거보니 반복문 돌려서 푸는 문제가 아니라 수식찾는 문제인듯)

#include <stdio.h>
#include <stdlib.h>

int main()
{
    int A, B, V;
    scanf("%d%d%d", &A, &B, &V);
    if (V == A)
        printf("1");
    else
    {
        int days = (V-B-1) / (A-B) + 1;
        printf("%d", days);
    }
}
